describe('tests', () => {
    it("should point to the current file name based on environment variables", () => {
        // SETUP
        const expectedFileName = "2024-01-02 Tuesday.md"

        // Mock Luxon DateTime


        // EXECUTE

        // ASSERT
    })
})